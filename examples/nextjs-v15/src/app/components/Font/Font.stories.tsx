import Font from "./Font";

export default {
  component: Font,
};

export const WithClassName = {
  args: {
    variant: "className",
  },
};

export const WithStyle = {
  args: {
    variant: "style",
  },
};

export const WithVariable = {
  args: {
    variant: "variable",
  },
};
